[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "compare-dicts-lib"
version = "0.2.0"
description = "A lightweight Python library for comparing two dictionaries and getting only the differences."
readme = "README.md"
license = {file = "LICENSE"}
authors = [
    {name = "Super Gabian", email = "supergabian13@gmail.com"}
]
dependencies = []
requires-python = ">=3.7"

[project.urls]
Homepage = "https://github.com/SuperGabian/compare_dicts_lib"
Repository = "https://github.com/SuperGabian/compare_dicts_lib"
