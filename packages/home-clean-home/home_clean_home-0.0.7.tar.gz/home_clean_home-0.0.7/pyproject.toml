[project]
name = "home-clean-home"
version = "0.0.7"
description = "MCP server which contains tools for Home Clean Home chatbot service."
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
 "mcp>=1.3.0",
 "openpyxl>=3.1.5",
 "pandas>=2.2.3",
]
[[project.authors]]
name = "Zi Min ER"
email = "erzimin@gmail.com"

[build-system]
requires = [ "hatchling",]
build-backend = "hatchling.build"

[project.scripts]
home-clean-home = "home_clean_home:main"
