[build-system]
requires = [
    "setuptools>=42",
    "wheel",
    "scikit-build",
    "cmake",        # Ensure a recent CMake is available
    "ninja"# Optionally, for fast builds
]
build-backend = "setuptools.build_meta"
