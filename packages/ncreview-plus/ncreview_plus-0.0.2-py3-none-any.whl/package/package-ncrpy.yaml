#------------------------------------------------------------------------------
# APR Configuration File
#
# Detailed APR documentation can be found here:
# https://code.arm.gov/distrib/release/blob/master/README.md
#------------------------------------------------------------------------------

comp: dsutil
package-name: ncreview_plus
default-prefix: /apps/ds
# architecture: noarch

build-script: package/apr_build.sh --py --env-clean

package-conda-env: true

include-items:
  - (package/apps/ds)/bin
  - (package/apps/ds)/conda-env
  - (package/apps/ds)/package/conda

scriptlet-installer:
  post: 'root'

author:
  name:         'Gabriel Gibler'
  organization: 'Battelle, PNNL'
  email:        'gabriel.gibler@pnnl.gov'
  phone:        '509-375-4513'

service-now:
  - 'Test package, build updates --  attempt 3245043732'

synopsis: >
    A tool to compare and/or summarize netCDF files

description: >
    A command-line tool with web display for visualizing netCDF summaries and
    comparisons

notification-list:
  - N/A

notes: >
    None.
