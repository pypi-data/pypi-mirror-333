[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "vto-frontend"
version = "0.0.1"
description = "Just a personal test app, not intended for use by anyone else"
readme = "README.md"
requires-python = ">= 3.13"
dependencies = [
    "Markdown>=3.7",  # for templatetags
]
license = "MIT"
license-files = ["LICEN[CS]E*"]

[project.urls]
# Repository = "https://github.com/venteto/vto-frontend"