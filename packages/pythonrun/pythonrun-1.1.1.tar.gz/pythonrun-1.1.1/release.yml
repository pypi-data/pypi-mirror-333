name: 发布 Python 包

on:
  push:
    tags:
      - 'v*'  # 匹配所有以v开头的标签，例如 v1.0.0

# 添加明确的权限设置
permissions:
  contents: write
  packages: write

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v3
      with:
        fetch-depth: 0  # 获取所有历史记录和标签
    
    - name: 设置 Python
      uses: actions/setup-python@v4
      with:
        python-version: '3.10'
    
    - name: 安装依赖
      run: |
        python -m pip install --upgrade pip
        pip install build twine wheel setuptools setuptools_scm
        if [ -f requirements-dev.txt ]; then pip install -r requirements-dev.txt; fi
    
    - name: 构建和检查包
      run: |
        python -m build
        twine check dist/*
    
    - name: 发布到 PyPI
      uses: pypa/gh-action-pypi-publish@release/v1
      with:
        password: ${{ secrets.PYPI_API_TOKEN }}
        skip_existing: true
    
    - name: 创建 GitHub Release
      uses: softprops/action-gh-release@v1
      with:
        files: |
          dist/*.whl
          dist/*.tar.gz
        generate_release_notes: true
        token: ${{ secrets.GITHUB_TOKEN }}  # 使用 token 参数而不是环境变量 