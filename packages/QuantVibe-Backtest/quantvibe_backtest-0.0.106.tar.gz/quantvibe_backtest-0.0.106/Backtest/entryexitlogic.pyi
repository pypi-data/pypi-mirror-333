import pandas as pd
import numpy as np

def entry_exit_threshold(df: pd.DataFrame, rolling_window: int, threshold: float, backtest_mode: str) -> list[int]:
    ...

def entry_exit_band(df: pd.DataFrame, rolling_window: int, backtest_mode: str) -> list[int]:
    ...
