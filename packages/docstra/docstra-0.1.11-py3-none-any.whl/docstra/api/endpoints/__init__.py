"""Docstra API endpoints."""

# Import all routers to make them available through this package
from docstra.api.endpoints import ingest, query, sessions, status

# If other endpoints are needed, import them here