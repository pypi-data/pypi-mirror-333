# from pyarrow.csv
#
# def get_chunksize(memory=
#
# def read_columns(fpath)
#    writer = None
#    for chunk in pd.rea
#    with pyarrow.csv.open_csv(fpath) as f:
#        for chunk in f:
#            if chunk is not None:
#                pd.Table.from_batches([chunk])
#            chunk = pd.Table.from_batches([chunk])
#            writer.write_table(chunk)
