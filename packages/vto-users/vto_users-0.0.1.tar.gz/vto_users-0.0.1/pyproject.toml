[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "vto-users"
version = "0.0.1"
description = "Just a personal test app, not intended for use by anyone else"
readme = "README.md"
requires-python = ">= 3.13"
dependencies = [
    # "django-timezone-field>=7.1",   # no autocomplete
]
license = "MIT"
license-files = ["LICEN[CS]E*"]

[project.urls]
Repository = "https://github.com/venteto/vto-users"