[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "chbencode"
version = "1.0.0"
authors = [{name = "Marko", email = "py.markop@icloud.com"}]
description = "Encode string data by zlib, base64 invert etc."
readme = "README.md"
requires-python = ">=3.7"
license = {text = "MIT"}
classifiers = [
    "Programming Language :: Python :: 3",
    "License :: OSI Approved :: MIT License",
    "Operating System :: OS Independent",
]

dependencies = [
    # твои зависимости
]

[project.urls]
"Homepage" = "https://github.com/py.marko/repository"
