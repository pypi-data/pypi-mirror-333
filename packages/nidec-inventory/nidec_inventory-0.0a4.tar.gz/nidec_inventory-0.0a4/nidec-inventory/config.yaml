---
config:
  # PATH REPOSITORIO DE PARÁMETROS
  #path_repo_nidec: '../REPO/Unidrive M Connect'

  # PATH FICHEROS DE RESULTADOS
  #path_results: '../OUT/'

  # EXTENSIÓN FICHERO PARÁMETROS
  ext_par_file: '.parfile'
  ext_excel: '.xlsx'
  ext_csv: '.csv'

  # NOMBRES DE FICHEROS EXCLUIDOS (sin extensión)
  excluded_files: ['.DS_Store', 'default']

  # NOMBRE DE DIRECTORIOS EXCLUIDOS
  excluded_directories: ['devices','parameter files']

  # ETIQUETA RAIZ
  root_tag: "ParameterFile"
...