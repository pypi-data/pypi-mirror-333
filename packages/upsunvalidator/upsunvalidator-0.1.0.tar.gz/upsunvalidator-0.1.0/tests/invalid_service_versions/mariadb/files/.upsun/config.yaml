applications:
    app:
        type: php:8.1
        hooks:
            build: |
                npm install
        web:
            commands:
                start: npm start

        relationships:
            db:

services:
    db:
        type: mariadb:100.55
