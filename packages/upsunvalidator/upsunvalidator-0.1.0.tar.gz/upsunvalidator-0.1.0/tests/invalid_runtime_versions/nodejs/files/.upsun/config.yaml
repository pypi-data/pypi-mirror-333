applications:
    app:
        type: nodejs:999.999
        hooks:
            build: |
                npm install
        web:
            commands:
                start: npm start

        relationships:
            db:

services:
    db:
        type: mariadb:10.4
