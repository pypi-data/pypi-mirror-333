applications:
    app:
        type: rust:1
        web:
            commands:
                start: ./target/release/app
            locations:
                "/":
                    root: "static"
                    allow: true

services:
    db:
        type: postgresql:155

routes:
    "https://{default}/":
        type: upstream
        upstream: "app"