"""
Numerical space for ADM1 parameter.
This defines a parametrization on R^30 of the parameter, avoiding the positivity requirement
for the parameter values (and hence simplifying tasks such as optimisation). Only a fraction
of ADM1 parameters are considered unknown, while the remaining parameters are fixed to th
 default values

This is given as a template to follow when interfacing anaerodig package to optimisation
packages
"""
