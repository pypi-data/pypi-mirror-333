[build-system]
requires = ["setuptools>=61.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "glebs-package"
version = "1.3.0"
description = "A custom package for language identification subpackage and ocr QA score calculation subpackage"
authors = [{ name = "Glebs Vinarskis", email = "gleb.vinarskis@gmail.com" }]
requires-python = ">=3.7"
dependencies = ["cython",
      "pybloomfiltermmap3",
      "huggingface_hub",
      "floret"]
