[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "atools-cli"
version = "0.1.0"
description = "Arne's CLI Tools"
readme = "README.md"
license = {file = "LICENSE"}
authors = [{name = "Arne Van Looveren", email = "arne@arnevanlooveren.be"}]
dependencies = [
  "requests"
]

[project.scripts]
atools = "atools.cli:main"
