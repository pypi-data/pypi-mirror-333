[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "Group1402122"
version = "0.2.1"
description = "A test package for learning!"
readme = "README.md"
license = {text = "MIT"}
authors = [{name = "Anton", email = "s235256@dtu.dk"}]
dependencies = []  # Add dependices here e.g ["numpy", "requests"]
