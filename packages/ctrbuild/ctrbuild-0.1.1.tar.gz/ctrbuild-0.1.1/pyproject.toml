[project]
name = "ctrbuild"
version = "0.1.1"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "click>=8.1.8",
    "pyyaml>=6.0.2",
    "semver>=3.0.2",
]

[tool.uv]
package = true

[project.scripts]
ctrbuild = "ctrbuild.cli:cli"
