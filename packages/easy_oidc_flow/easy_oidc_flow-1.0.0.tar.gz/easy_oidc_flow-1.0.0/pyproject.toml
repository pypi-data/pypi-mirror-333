[project]
name = "easy_oidc_flow"
version = "1.0.0"
description = "a Flask wrapper which handles OIDC authentication, authorization, and the OAuth flow for a given Identity Provider."
readme = "README.md"
requires-python = ">=3.12"
dependencies = []
