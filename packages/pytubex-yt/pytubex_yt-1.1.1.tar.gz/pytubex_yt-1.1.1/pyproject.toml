[project]
name = "pytubex-yt"
version = "1.1.1"
description = "A CLI-based YouTube video & audio downloader"
authors = [{ name = "JfrzxCode" }]
readme = "README.md"
license = { text = "MIT" }
requires-python = ">=3.6"
dependencies = [
    "yt-dlp",
    "requests",
    "colorama"
]