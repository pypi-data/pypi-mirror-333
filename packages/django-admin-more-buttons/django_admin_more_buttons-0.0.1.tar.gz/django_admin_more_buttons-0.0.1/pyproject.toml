[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "django-admin-more-buttons"
version = "0.0.1"
authors = [
  { name="João Seckler", email="jseckler@riseup.net" },
]
description = "Add more buttons with custom behavior to your django admin's submit line."
readme = "README.md"
requires-python = ">=3.7"
classifiers = [
    "Programming Language :: Python :: 3",
    "Operating System :: OS Independent",
]
license = "MIT"
license-files = ["LICEN[CS]E*"]

[project.urls]
Homepage = "https://gitlab.com/joaoseckler/django-admin-more-buttons"
Issues = "https://gitlab.com/joaoseckler/django-admin-more-buttons/issues"

[tool.hatch.build.targets.wheel]
packages = ["admin_buttons"]
