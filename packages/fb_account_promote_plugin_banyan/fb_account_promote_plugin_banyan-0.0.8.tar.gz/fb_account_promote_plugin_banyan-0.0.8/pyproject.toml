[project]
name = "fb_account_promote_plugin_banyan"
version = "0.0.8"
description = ""
authors = [
    {name = "chuck yang",email = "yzhang.baby@gmail.com"}
]
readme = "README.md"
requires-python = ">=3.11, <4.0"
dynamic = ["dependencies"]

[project.entry-points]
facebook.account_promotion = "fb_account_promote_plugin_banyan.account_promotion:PluginA"

[tool.poetry]
packages = [{include = "fb_account_promote_plugin_banyan", from = "src"}]


[tool.poetry.dependencies]
xcmap = "^0.0.1"

[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"
