[project]
name = "dummygen-cli"
version = "0.1.2"
description = "A simple Python script to generate dummy file of specified size."
readme = "README.md"
requires-python = ">=3.8"
dependencies = [
    "rich>=13.9.4",
]
