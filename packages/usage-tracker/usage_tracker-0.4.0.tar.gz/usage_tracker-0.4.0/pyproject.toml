[tool.poetry]
name = "usage-tracker"
version = "0.4.0"
description = "A simple library to track service usage, including LLM token counts."
authors = ["Your Name <you@example.com>"]
license = "MIT"

[tool.poetry.dependencies]
python = "^3.7"
pymysql = "^1.1.1"

[build-system]
requires = ["poetry-core>=1.0.0"]
build-backend = "poetry.core.masonry.api"
