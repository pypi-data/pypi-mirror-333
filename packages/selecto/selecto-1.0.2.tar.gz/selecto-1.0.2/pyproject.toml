[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "selecto"
version = "1.0.2"
description = "A simple TUI option selector"
authors = [{name = "Manuel Germanos"}]
license = {text = "MIT"}
readme = "README.md"
requires-python = ">=3.6"
dependencies = []

[tool.setuptools]
packages = ["selecto"]
package-dir = {"" = "src"}