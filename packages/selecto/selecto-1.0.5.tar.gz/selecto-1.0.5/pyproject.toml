[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"

[metadata]
version = "1.0.5"
description = "Simple, colorful, multi-mode TUI that requires no boilerplate"
readme = "README.md"
authors = ["Manuel Germanos"]