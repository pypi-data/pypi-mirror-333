[project]
name = "lmos-openai-types"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "TerminalMan", email = "84923604+SecretiveShell@users.noreply.github.com" }
]
requires-python = ">=3.13"
dependencies = [
    "pydantic>=2.10.6",
]

[project.scripts]
lmos-openai-types = "lmos_openai_types:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
