[tool.poetry]
name = "mcp-playwright"
version = "0.1.0"
description = ""
authors = ["Pony.Ma <mtf201013@gmail.com>"]
readme = "README.md"
packages = [
    { include = "mcp_playwright", from = "." }
]

[tool.poetry.dependencies]
python = "^3.10"
playwright = {extras = ["all"], version = "^1.50.0"}


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
