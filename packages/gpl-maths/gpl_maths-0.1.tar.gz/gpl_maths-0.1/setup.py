from setuptools import setup,find_packages

setup(
    name ="gpl_maths",
    version= "0.1",
    author = 'Suresh Maha Rajashekar',
    packages = find_packages(),
    install_requires=['numpy'],

)