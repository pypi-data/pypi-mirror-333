[project]
name = "aiscout"
version = "0.1.0"
description = "A lightning fast and insanely accurate agentic object detection system"
readme = "README.md"
requires-python = ">=3.9"
dependencies = [
    "ultralytics",
    "requests",
    "rich",
]
authors = [{name = "Jacob Weiss", email = "jaweiss2305@gmail.com"}]

[build-system]
requires = ["setuptools>=42", "wheel"]
build-backend = "setuptools.build_meta"